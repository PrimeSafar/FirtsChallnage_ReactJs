ternoray opertor 
import "./BUTTON.css"
export default function Button({title}){
    return(
        <div>
        {title == null || title == "" ? <div></div> :<button>{title}</button>}
        
            </div>
    );
}












 if(title == null || title == ""){
        return null
    }
    return(
        <button>
                {title}
            </button>
    );